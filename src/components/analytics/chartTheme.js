export const CHART = {
  stroke: 'var(--fg)',
  grid: 'var(--border-color)',
  fill: 'var(--fg)',
  muted: 'var(--muted)',
};

export const chartMargins = { top: 8, right: 8, left: -20, bottom: 0 };

export const axisTick = { fill: 'var(--muted)', fontSize: 9 };
