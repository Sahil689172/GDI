# Gotta-do-it — Production Cleanup Report

**Date:** 2026-05-29  
**Scope:** Dead code removal, script fixes, maintainability — no UI or behavior changes.

---

## 1. Summary

| Metric | Result |
|--------|--------|
| Files deleted | 9 |
| Routes removed | 1 duplicate (`GET /api/focus/sessions`) |
| npm packages removed | 0 (all in use or build tooling) |
| Frontend production build | Passed (`npm run build`) |
| Backend module load | Passed (routes import) |
| PWA artifacts | Generated (`dist/sw.js`, precache 45 entries) |

---

## 2. Implemented cleanup

### Removed files (safe — no references)

| File | Reason |
|------|--------|
| `src/hooks/useMemoContextValue.js` | Never imported |
| `src/hooks/index.js` | Barrel unused; all hooks imported by direct path |
| `src/components/motion/MotionButton.jsx` | Orphan component |
| `src/components/motion/ScrollReveal.jsx` | Orphan component |
| `src/components/GlobalSearch.jsx` | Thin re-export; `AppLayout` now lazy-loads `CommandPalette` |
| `electron/index.js` | Unused shim (`main` is `electron/main.js` in root `package.json`) |
| `electron/assets/icon.svg` | Obsolete clock icon; branding uses `assets/brand/lightning.svg` + generated PNGs |
| `electron/renderer-bridge.d.ts` | Unused preload typings |

### Trimmed exports

- `src/hooks/useWindowSize.js` — removed unused `useIsMobile` / `useIsTablet` (app uses `useIsMobileLayout` from `useMediaQuery.js`).

### Backend

- `backend/src/routes/focusRoutes.js` — removed duplicate `GET /sessions` (same handler as `GET /`). Frontend uses `GET /api/focus` via `focusApi.fetchSessions()`.

### Dev scripts (Electron reliability)

| Script | Behavior |
|--------|----------|
| `npm run desktop` | Wait for Vite `:3000` only, then launch Electron |
| `npm run desktop:api` | Wait for Vite + API health, then launch Electron |
| `npm run desktop:dev` | Web + `desktop` (no API required) |
| `npm run desktop:all` | API + web + `desktop:api` |

Previously `desktop:dev` waited forever for API health while the API was never started.

---

## 3. Audit — kept intentionally

### All pages routed

`Home`, `Tasks`, `Goals`, `Focus`, `Calendar`, `Analytics`, `Profile`, `Login`, `Signup` — lazy-loaded in `AppRoutes.jsx`.

### All contexts wired

`AppProviders.jsx`: Auth → App → Sync → Tasks → Goals → Focus → Calendar → Dashboard → Notifications.

### All frontend services in use

`authApi`, `tasksApi`, `goalsApi`, `focusApi`, `analyticsApi`, `notificationsApi`, `syncApi`.

### Console output

- **Frontend `src/`:** No `console.log` / `debug` found.
- **Backend:** Structured startup / MongoDB logs in `index.js`, `db.js`, and `db:verify` script only — appropriate for production ops.

### Duplicate logic (not removed — document only)

| Area | Notes |
|------|--------|
| `goalProgress.js` | Exists in frontend (`src/utils`) and backend (`backend/src/utils`). Intentional mirror; consolidate later behind API-only source of truth. |
| Mobile vs desktop dashboard | `Dashboard.jsx` + `DashboardMobile.jsx` — parallel UI, not duplicate dead code. |

### Large dependencies (keep — feature-driven)

| Package | Used by |
|---------|---------|
| `recharts` | Analytics charts |
| `react-big-calendar` | Calendar page |
| `@dnd-kit/*` | Task board drag-and-drop |
| `framer-motion` | Transitions, loading, UI motion |
| `vite-plugin-pwa` | PWA / service worker |

---

## 4. Files safe to delete (manual / disk only)

These are **not** in git or are build output — safe to remove locally to reclaim space:

| Path | Notes |
|------|--------|
| `dist/` | Regenerated by `npm run build` |
| `release/` | Electron builder output |
| `node_modules/` | Reinstall with `npm install` |

**Do not delete:** `public/icons/*`, `public/favicon.ico`, `electron/assets/icon*.png` (runtime / packaging).

---

## 5. Packages safe to uninstall

**None recommended** after `depcheck` review:

- `@types/react`, `@types/react-dom` — IDE/type tooling (depcheck false positive).
- `tailwindcss`, `postcss`, `autoprefixer` — used via config, not direct imports.
- All runtime deps are referenced by features (calendar, charts, DnD, PWA, Electron tooling).

`virtual:pwa-register` is a Vite virtual module — not an npm package.

---

## 6. Refactoring suggestions (future, non-breaking)

1. **Unify goal progress** — Single module or API-only progress; remove drift between client/server `goalProgress.js`.
2. **Split vendor chunks further** — Optional `manualChunks` for `lucide-react` tree-shaking audit.
3. **Lazy-load Recharts** — Analytics route already lazy; charts could use `React.lazy` per chart if bundle size becomes an issue (~383 KB gzip chunk today).
4. **Remove empty `src/components/motion/`** folder if still present after git commit.
5. **JWT_SECRET** — Set a strong secret in `backend/.env` for production (config already warns in dev).
6. **Consolidate dashboard layouts** — Shared hooks between `Dashboard` and `DashboardMobile` to reduce duplication without changing UX.

---

## 7. Verification performed

| Check | Command / result |
|-------|------------------|
| Frontend build | `npm run build` — success, PWA SW generated |
| Broken imports | Grep: no references to deleted symbols |
| Backend routes | `node -e "import('./src/routes/index.js')"` — success |
| Electron entry | Root `"main": "electron/main.js"` unchanged |
| PWA | `vite-plugin-pwa` precache in build output |

### Recommended manual smoke tests

```bash
npm run dev:all          # Web + API
npm run desktop:all      # Full desktop stack
npm run preview          # Production build locally
cd backend && npm run db:verify   # MongoDB (with .env)
```

---

## 8. What was not changed

- UI layout, styling, or user flows
- Auth, sync, notifications, MongoDB models
- Electron preload / security model
- PWA manifest and service worker registration
